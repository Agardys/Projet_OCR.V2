# Simple SDL mini Makefile

CC=gcc

CPPFLAGS= `pkg-config --cflags sdl` -MMD
CFLAGS= -std=c99 -O3
LDFLAGS=

all: main

main: main.o neural_network.o

clean:
	${RM} *.o
	${RM} *.d

fclean: clean
	${RM} main

# END
